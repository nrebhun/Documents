//	p1dat.p8	1996 apr 14	last modified 1997 mar 26
//
//	This program consists of all the legal p1 assignment statements.
//	You might like to compile this; then change "float" to "int",
//	and compile again.
//

{
	float a,b,c,d,e,f,g,h,i,j,k,u,v,w,x,y,z;

	a = b+c-d/e;
	x = y;
	z = y*(x-w/(v-u));
	a = a-((b+c)*d-e)/((f-g)*h-(i-j/k));
	a = b+c*d;
	a = b*c+d;
	a = a-b/(c-d);
	a = (a-b)/(c*(x-y/z)/(x-z/(x-y))+(x-y));
}
