#include <stdio.h>

int main(void)
{
	printf("hey whats up?");
	while(getchar() != '1')
		printf("cool!... but youre going to be here for a while... so tell me more!");
printf("ok, bye");

return 0;
}